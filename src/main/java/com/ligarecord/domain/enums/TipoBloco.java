package com.ligarecord.domain.enums;

public enum TipoBloco {
    /** Cobrada uma única vez, quando a equipa entra na liga. */
    INSCRICAO,
    /** Cobrada periodicamente, por escalão de classificação. */
    PERIODO
}
